const SectionHead = ({title}) => {
    return(
        <div className="section__head">
            <div className='justify-center text-center'>
            <h4 class="text-monospace text-muted">{title}</h4>
            </div>
        </div>
    )
}
export default SectionHead